VITE_API_URL=http://localhost:8080
# Empty on purpose: posthog-js boots as soon as both are set, so a placeholder
# would point every copy of this file at a host that does not resolve.
VITE_POSTHOG_KEY=
VITE_POSTHOG_HOST=
