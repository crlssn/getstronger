VITE_API_URL=http://localhost:8080
# Analytics run in the deployed app only, which takes these from the deploy
# workflow's repository variables. A local build leaves them empty.
VITE_POSTHOG_KEY=
VITE_POSTHOG_HOST=
