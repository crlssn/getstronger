// The mailbox both policy pages point people at. Not a catalogue string: an
// address is the same in every locale, and a translator must not be able to
// alter it. Shared, because the privacy policy and the deletion page naming
// different addresses is a store rejection waiting to happen.
export const privacyEmail = 'privacy@getstronger.studio'
