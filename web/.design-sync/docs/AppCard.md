---
category: Surfaces
---

The panel: rounded, on `--color-surface`, lifted by the card shadow. No border
— elevation and a hairline never combine, so a top-level card takes the shadow
and a container nested inside one closes its edge instead.

A component that styles its own container applies the `card` utility in its CSS
module: the same shape, defined once.
