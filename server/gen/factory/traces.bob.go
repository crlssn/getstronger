// Code generated by BobGen psql v0.50.0. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package factory

import (
	"context"
	"testing"
	"time"

	"github.com/aarondl/opt/omit"
	models "github.com/crlssn/getstronger/server/gen/models"
	"github.com/gofrs/uuid/v5"
	"github.com/jaswdr/faker/v2"
	"github.com/stephenafamo/bob"
)

type TraceMod interface {
	Apply(context.Context, *TraceTemplate)
}

type TraceModFunc func(context.Context, *TraceTemplate)

func (f TraceModFunc) Apply(ctx context.Context, n *TraceTemplate) {
	f(ctx, n)
}

type TraceModSlice []TraceMod

func (mods TraceModSlice) Apply(ctx context.Context, n *TraceTemplate) {
	for _, f := range mods {
		f.Apply(ctx, n)
	}
}

// TraceTemplate is an object representing the database table.
// all columns are optional and should be set by mods
type TraceTemplate struct {
	ID         func() uuid.UUID
	Request    func() string
	StatusCode func() int32
	DurationMS func() int32
	CreatedAt  func() time.Time

	f *Factory

	alreadyPersisted bool
}

// Apply mods to the TraceTemplate
func (o *TraceTemplate) Apply(ctx context.Context, mods ...TraceMod) {
	for _, mod := range mods {
		mod.Apply(ctx, o)
	}
}

// setModelRels creates and sets the relationships on *models.Trace
// according to the relationships in the template. Nothing is inserted into the db
func (t TraceTemplate) setModelRels(o *models.Trace) {}

// BuildSetter returns an *models.TraceSetter
// this does nothing with the relationship templates
func (o TraceTemplate) BuildSetter() *models.TraceSetter {
	m := &models.TraceSetter{}

	if o.ID != nil {
		val := o.ID()
		m.ID = omit.From(val)
	}
	if o.Request != nil {
		val := o.Request()
		m.Request = omit.From(val)
	}
	if o.StatusCode != nil {
		val := o.StatusCode()
		m.StatusCode = omit.From(val)
	}
	if o.DurationMS != nil {
		val := o.DurationMS()
		m.DurationMS = omit.From(val)
	}
	if o.CreatedAt != nil {
		val := o.CreatedAt()
		m.CreatedAt = omit.From(val)
	}

	return m
}

// BuildManySetter returns an []*models.TraceSetter
// this does nothing with the relationship templates
func (o TraceTemplate) BuildManySetter(number int) []*models.TraceSetter {
	m := make([]*models.TraceSetter, number)

	for i := range m {
		m[i] = o.BuildSetter()
	}

	return m
}

// Build returns an *models.Trace
// Related objects are also created and placed in the .R field
// NOTE: Objects are not inserted into the database. Use TraceTemplate.Create
func (o TraceTemplate) Build() *models.Trace {
	m := &models.Trace{}

	if o.ID != nil {
		m.ID = o.ID()
	}
	if o.Request != nil {
		m.Request = o.Request()
	}
	if o.StatusCode != nil {
		m.StatusCode = o.StatusCode()
	}
	if o.DurationMS != nil {
		m.DurationMS = o.DurationMS()
	}
	if o.CreatedAt != nil {
		m.CreatedAt = o.CreatedAt()
	}

	o.setModelRels(m)

	return m
}

// BuildMany returns an models.TraceSlice
// Related objects are also created and placed in the .R field
// NOTE: Objects are not inserted into the database. Use TraceTemplate.CreateMany
func (o TraceTemplate) BuildMany(number int) models.TraceSlice {
	m := make(models.TraceSlice, number)

	for i := range m {
		m[i] = o.Build()
	}

	return m
}

func ensureCreatableTrace(m *models.TraceSetter) {
	if m.Request.IsUnset() {
		val := random_string(nil)
		m.Request = omit.From(val)
	}
	if m.StatusCode.IsUnset() {
		val := random_int32(nil)
		m.StatusCode = omit.From(val)
	}
	if m.DurationMS.IsUnset() {
		val := random_int32(nil)
		m.DurationMS = omit.From(val)
	}
}

// insertOptRels creates and inserts any optional the relationships on *models.Trace
// according to the relationships in the template.
// any required relationship should have already exist on the model
func (o *TraceTemplate) insertOptRels(ctx context.Context, exec bob.Executor, m *models.Trace) error {
	var err error

	return err
}

// Create builds a trace and inserts it into the database
// Relations objects are also inserted and placed in the .R field
func (o *TraceTemplate) Create(ctx context.Context, exec bob.Executor) (*models.Trace, error) {
	var err error
	opt := o.BuildSetter()
	ensureCreatableTrace(opt)

	// Retrieve ancestor models from context to avoid duplicate parent creation.
	// Parents are keyed by "parent_table:child_table:child_rel_name".
	// This works regardless of NoBackReferencing since it only uses child-side metadata.
	mInCreation, _ := modelsInCreationCtx.Value(ctx)

	m, err := models.Traces.Insert(opt).One(ctx, exec)
	if err != nil {
		return nil, err
	}

	// Store this model in context for child creates.
	// Key format: "parent_table:child_table:child_rel_name" where child_rel_name is the FK name.
	// We store an entry for every relationship pointing TO this table (where this table is the parent).
	// This works regardless of NoBackReferencing since keys use child-side metadata.
	newMInCreation := make(map[string]any, len(mInCreation)+1)
	for k, v := range mInCreation {
		newMInCreation[k] = v
	}

	ctx = modelsInCreationCtx.WithValue(ctx, newMInCreation)

	if err := o.insertOptRels(ctx, exec, m); err != nil {
		return nil, err
	}
	return m, err
}

// MustCreate builds a trace and inserts it into the database
// Relations objects are also inserted and placed in the .R field
// panics if an error occurs
func (o *TraceTemplate) MustCreate(ctx context.Context, exec bob.Executor) *models.Trace {
	m, err := o.Create(ctx, exec)
	if err != nil {
		panic(err)
	}
	return m
}

// CreateOrFail builds a trace and inserts it into the database
// Relations objects are also inserted and placed in the .R field
// It calls `tb.Fatal(err)` on the test/benchmark if an error occurs
func (o *TraceTemplate) CreateOrFail(ctx context.Context, tb testing.TB, exec bob.Executor) *models.Trace {
	tb.Helper()
	m, err := o.Create(ctx, exec)
	if err != nil {
		tb.Fatal(err)
		return nil
	}
	return m
}

// CreateMany builds multiple traces and inserts them into the database
// Relations objects are also inserted and placed in the .R field
func (o TraceTemplate) CreateMany(ctx context.Context, exec bob.Executor, number int) (models.TraceSlice, error) {
	var err error
	m := make(models.TraceSlice, number)

	for i := range m {
		m[i], err = o.Create(ctx, exec)
		if err != nil {
			return nil, err
		}
	}

	return m, nil
}

// MustCreateMany builds multiple traces and inserts them into the database
// Relations objects are also inserted and placed in the .R field
// panics if an error occurs
func (o TraceTemplate) MustCreateMany(ctx context.Context, exec bob.Executor, number int) models.TraceSlice {
	m, err := o.CreateMany(ctx, exec, number)
	if err != nil {
		panic(err)
	}
	return m
}

// CreateManyOrFail builds multiple traces and inserts them into the database
// Relations objects are also inserted and placed in the .R field
// It calls `tb.Fatal(err)` on the test/benchmark if an error occurs
func (o TraceTemplate) CreateManyOrFail(ctx context.Context, tb testing.TB, exec bob.Executor, number int) models.TraceSlice {
	tb.Helper()
	m, err := o.CreateMany(ctx, exec, number)
	if err != nil {
		tb.Fatal(err)
		return nil
	}
	return m
}

// Trace has methods that act as mods for the TraceTemplate
var TraceMods traceMods

type traceMods struct{}

func (m traceMods) RandomizeAllColumns(f *faker.Faker) TraceMod {
	return TraceModSlice{
		TraceMods.RandomID(f),
		TraceMods.RandomRequest(f),
		TraceMods.RandomStatusCode(f),
		TraceMods.RandomDurationMS(f),
		TraceMods.RandomCreatedAt(f),
	}
}

// Set the model columns to this value
func (m traceMods) ID(val uuid.UUID) TraceMod {
	return TraceModFunc(func(_ context.Context, o *TraceTemplate) {
		o.ID = func() uuid.UUID { return val }
	})
}

// Set the Column from the function
func (m traceMods) IDFunc(f func() uuid.UUID) TraceMod {
	return TraceModFunc(func(_ context.Context, o *TraceTemplate) {
		o.ID = f
	})
}

// Clear any values for the column
func (m traceMods) UnsetID() TraceMod {
	return TraceModFunc(func(_ context.Context, o *TraceTemplate) {
		o.ID = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m traceMods) RandomID(f *faker.Faker) TraceMod {
	return TraceModFunc(func(_ context.Context, o *TraceTemplate) {
		o.ID = func() uuid.UUID {
			return random_uuid_UUID(f)
		}
	})
}

// Set the model columns to this value
func (m traceMods) Request(val string) TraceMod {
	return TraceModFunc(func(_ context.Context, o *TraceTemplate) {
		o.Request = func() string { return val }
	})
}

// Set the Column from the function
func (m traceMods) RequestFunc(f func() string) TraceMod {
	return TraceModFunc(func(_ context.Context, o *TraceTemplate) {
		o.Request = f
	})
}

// Clear any values for the column
func (m traceMods) UnsetRequest() TraceMod {
	return TraceModFunc(func(_ context.Context, o *TraceTemplate) {
		o.Request = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m traceMods) RandomRequest(f *faker.Faker) TraceMod {
	return TraceModFunc(func(_ context.Context, o *TraceTemplate) {
		o.Request = func() string {
			return random_string(f)
		}
	})
}

// Set the model columns to this value
func (m traceMods) StatusCode(val int32) TraceMod {
	return TraceModFunc(func(_ context.Context, o *TraceTemplate) {
		o.StatusCode = func() int32 { return val }
	})
}

// Set the Column from the function
func (m traceMods) StatusCodeFunc(f func() int32) TraceMod {
	return TraceModFunc(func(_ context.Context, o *TraceTemplate) {
		o.StatusCode = f
	})
}

// Clear any values for the column
func (m traceMods) UnsetStatusCode() TraceMod {
	return TraceModFunc(func(_ context.Context, o *TraceTemplate) {
		o.StatusCode = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m traceMods) RandomStatusCode(f *faker.Faker) TraceMod {
	return TraceModFunc(func(_ context.Context, o *TraceTemplate) {
		o.StatusCode = func() int32 {
			return random_int32(f)
		}
	})
}

// Set the model columns to this value
func (m traceMods) DurationMS(val int32) TraceMod {
	return TraceModFunc(func(_ context.Context, o *TraceTemplate) {
		o.DurationMS = func() int32 { return val }
	})
}

// Set the Column from the function
func (m traceMods) DurationMSFunc(f func() int32) TraceMod {
	return TraceModFunc(func(_ context.Context, o *TraceTemplate) {
		o.DurationMS = f
	})
}

// Clear any values for the column
func (m traceMods) UnsetDurationMS() TraceMod {
	return TraceModFunc(func(_ context.Context, o *TraceTemplate) {
		o.DurationMS = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m traceMods) RandomDurationMS(f *faker.Faker) TraceMod {
	return TraceModFunc(func(_ context.Context, o *TraceTemplate) {
		o.DurationMS = func() int32 {
			return random_int32(f)
		}
	})
}

// Set the model columns to this value
func (m traceMods) CreatedAt(val time.Time) TraceMod {
	return TraceModFunc(func(_ context.Context, o *TraceTemplate) {
		o.CreatedAt = func() time.Time { return val }
	})
}

// Set the Column from the function
func (m traceMods) CreatedAtFunc(f func() time.Time) TraceMod {
	return TraceModFunc(func(_ context.Context, o *TraceTemplate) {
		o.CreatedAt = f
	})
}

// Clear any values for the column
func (m traceMods) UnsetCreatedAt() TraceMod {
	return TraceModFunc(func(_ context.Context, o *TraceTemplate) {
		o.CreatedAt = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m traceMods) RandomCreatedAt(f *faker.Faker) TraceMod {
	return TraceModFunc(func(_ context.Context, o *TraceTemplate) {
		o.CreatedAt = func() time.Time {
			return random_time_Time(f)
		}
	})
}

func (m traceMods) WithParentsCascading() TraceMod {
	return TraceModFunc(func(ctx context.Context, o *TraceTemplate) {
		if isDone, _ := traceWithParentsCascadingCtx.Value(ctx); isDone {
			return
		}
		ctx = traceWithParentsCascadingCtx.WithValue(ctx, true)
	})
}
