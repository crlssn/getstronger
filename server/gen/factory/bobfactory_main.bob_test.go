// Code generated by BobGen psql v0.50.0. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package factory

import (
	"context"
	"testing"

	models "github.com/crlssn/getstronger/server/gen/models"
)

func TestCreateAuth(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	if _, err := New().NewAuthWithContext(ctx).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating Auth: %v", err)
	}
}

func TestCreateAuthRateLimit(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	if _, err := New().NewAuthRateLimitWithContext(ctx).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating AuthRateLimit: %v", err)
	}
}

func TestCreateEvent(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	if _, err := New().NewEventWithContext(ctx).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating Event: %v", err)
	}
}

func TestCreateExercise(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	if _, err := New().NewExerciseWithContext(ctx).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating Exercise: %v", err)
	}
}

func TestCreateExerciseWithExercisesRoutinesDoesNotDuplicateParent(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	before, err := models.Exercises.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Exercises: %v", err)
	}

	if _, err := New().NewExerciseWithContext(ctx, ExerciseMods.WithNewExercisesRoutines(2)).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating Exercise with ExercisesRoutines: %v", err)
	}

	after, err := models.Exercises.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Exercises: %v", err)
	}

	if got := after - before; got != 1 {
		t.Fatalf("Expected Exercises to increase by 1, got %d", got)
	}
}

func TestCreateExerciseWithSetsDoesNotDuplicateParent(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	before, err := models.Exercises.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Exercises: %v", err)
	}

	if _, err := New().NewExerciseWithContext(ctx, ExerciseMods.WithNewSets(2)).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating Exercise with Sets: %v", err)
	}

	after, err := models.Exercises.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Exercises: %v", err)
	}

	if got := after - before; got != 1 {
		t.Fatalf("Expected Exercises to increase by 1, got %d", got)
	}
}

func TestCreateExerciseWithWorkoutGroupExercisesDoesNotDuplicateParent(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	before, err := models.Exercises.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Exercises: %v", err)
	}

	if _, err := New().NewExerciseWithContext(ctx, ExerciseMods.WithNewWorkoutGroupExercises(2)).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating Exercise with WorkoutGroupExercises: %v", err)
	}

	after, err := models.Exercises.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Exercises: %v", err)
	}

	if got := after - before; got != 1 {
		t.Fatalf("Expected Exercises to increase by 1, got %d", got)
	}
}

func TestCreateExercisesRoutine(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	if _, err := New().NewExercisesRoutineWithContext(ctx).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating ExercisesRoutine: %v", err)
	}
}

func TestCreateFollower(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	if _, err := New().NewFollowerWithContext(ctx).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating Follower: %v", err)
	}
}

func TestCreateNotification(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	if _, err := New().NewNotificationWithContext(ctx).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating Notification: %v", err)
	}
}

func TestCreatePlanRoutine(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	if _, err := New().NewPlanRoutineWithContext(ctx).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating PlanRoutine: %v", err)
	}
}

func TestCreatePlan(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	if _, err := New().NewPlanWithContext(ctx).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating Plan: %v", err)
	}
}

func TestCreatePlanWithPlanRoutinesDoesNotDuplicateParent(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	before, err := models.Plans.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Plans: %v", err)
	}

	if _, err := New().NewPlanWithContext(ctx, PlanMods.WithNewPlanRoutines(2)).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating Plan with PlanRoutines: %v", err)
	}

	after, err := models.Plans.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Plans: %v", err)
	}

	if got := after - before; got != 1 {
		t.Fatalf("Expected Plans to increase by 1, got %d", got)
	}
}

func TestCreateRoutineGroup(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	if _, err := New().NewRoutineGroupWithContext(ctx).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating RoutineGroup: %v", err)
	}
}

func TestCreateRoutineGroupWithGroupExercisesRoutinesDoesNotDuplicateParent(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	before, err := models.RoutineGroups.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting RoutineGroups: %v", err)
	}

	if _, err := New().NewRoutineGroupWithContext(ctx, RoutineGroupMods.WithNewGroupExercisesRoutines(2)).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating RoutineGroup with GroupExercisesRoutines: %v", err)
	}

	after, err := models.RoutineGroups.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting RoutineGroups: %v", err)
	}

	if got := after - before; got != 1 {
		t.Fatalf("Expected RoutineGroups to increase by 1, got %d", got)
	}
}

func TestCreateRoutine(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	if _, err := New().NewRoutineWithContext(ctx).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating Routine: %v", err)
	}
}

func TestCreateRoutineWithExercisesRoutinesDoesNotDuplicateParent(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	before, err := models.Routines.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Routines: %v", err)
	}

	if _, err := New().NewRoutineWithContext(ctx, RoutineMods.WithNewExercisesRoutines(2)).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating Routine with ExercisesRoutines: %v", err)
	}

	after, err := models.Routines.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Routines: %v", err)
	}

	if got := after - before; got != 1 {
		t.Fatalf("Expected Routines to increase by 1, got %d", got)
	}
}

func TestCreateRoutineWithPlanRoutinesDoesNotDuplicateParent(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	before, err := models.Routines.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Routines: %v", err)
	}

	if _, err := New().NewRoutineWithContext(ctx, RoutineMods.WithNewPlanRoutines(2)).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating Routine with PlanRoutines: %v", err)
	}

	after, err := models.Routines.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Routines: %v", err)
	}

	if got := after - before; got != 1 {
		t.Fatalf("Expected Routines to increase by 1, got %d", got)
	}
}

func TestCreateRoutineWithRoutineGroupsDoesNotDuplicateParent(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	before, err := models.Routines.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Routines: %v", err)
	}

	if _, err := New().NewRoutineWithContext(ctx, RoutineMods.WithNewRoutineGroups(2)).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating Routine with RoutineGroups: %v", err)
	}

	after, err := models.Routines.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Routines: %v", err)
	}

	if got := after - before; got != 1 {
		t.Fatalf("Expected Routines to increase by 1, got %d", got)
	}
}

func TestCreateRoutineWithWorkoutsDoesNotDuplicateParent(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	before, err := models.Routines.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Routines: %v", err)
	}

	if _, err := New().NewRoutineWithContext(ctx, RoutineMods.WithNewWorkouts(2)).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating Routine with Workouts: %v", err)
	}

	after, err := models.Routines.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Routines: %v", err)
	}

	if got := after - before; got != 1 {
		t.Fatalf("Expected Routines to increase by 1, got %d", got)
	}
}

func TestCreateSet(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	if _, err := New().NewSetWithContext(ctx).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating Set: %v", err)
	}
}

func TestCreateTrace(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	if _, err := New().NewTraceWithContext(ctx).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating Trace: %v", err)
	}
}

func TestCreateUser(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	if _, err := New().NewUserWithContext(ctx).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating User: %v", err)
	}
}

func TestCreateUserWithExercisesDoesNotDuplicateParent(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	before, err := models.Users.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Users: %v", err)
	}

	if _, err := New().NewUserWithContext(ctx, UserMods.WithNewExercises(2)).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating User with Exercises: %v", err)
	}

	after, err := models.Users.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Users: %v", err)
	}

	if got := after - before; got != 1 {
		t.Fatalf("Expected Users to increase by 1, got %d", got)
	}
}

func TestCreateUserWithNotificationsDoesNotDuplicateParent(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	before, err := models.Users.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Users: %v", err)
	}

	if _, err := New().NewUserWithContext(ctx, UserMods.WithNewNotifications(2)).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating User with Notifications: %v", err)
	}

	after, err := models.Users.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Users: %v", err)
	}

	if got := after - before; got != 1 {
		t.Fatalf("Expected Users to increase by 1, got %d", got)
	}
}

func TestCreateUserWithPlansDoesNotDuplicateParent(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	before, err := models.Users.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Users: %v", err)
	}

	if _, err := New().NewUserWithContext(ctx, UserMods.WithNewPlans(2)).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating User with Plans: %v", err)
	}

	after, err := models.Users.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Users: %v", err)
	}

	if got := after - before; got != 1 {
		t.Fatalf("Expected Users to increase by 1, got %d", got)
	}
}

func TestCreateUserWithRoutinesDoesNotDuplicateParent(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	before, err := models.Users.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Users: %v", err)
	}

	if _, err := New().NewUserWithContext(ctx, UserMods.WithNewRoutines(2)).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating User with Routines: %v", err)
	}

	after, err := models.Users.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Users: %v", err)
	}

	if got := after - before; got != 1 {
		t.Fatalf("Expected Users to increase by 1, got %d", got)
	}
}

func TestCreateUserWithSetsDoesNotDuplicateParent(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	before, err := models.Users.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Users: %v", err)
	}

	if _, err := New().NewUserWithContext(ctx, UserMods.WithNewSets(2)).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating User with Sets: %v", err)
	}

	after, err := models.Users.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Users: %v", err)
	}

	if got := after - before; got != 1 {
		t.Fatalf("Expected Users to increase by 1, got %d", got)
	}
}

func TestCreateUserWithWorkoutCommentsDoesNotDuplicateParent(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	before, err := models.Users.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Users: %v", err)
	}

	if _, err := New().NewUserWithContext(ctx, UserMods.WithNewWorkoutComments(2)).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating User with WorkoutComments: %v", err)
	}

	after, err := models.Users.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Users: %v", err)
	}

	if got := after - before; got != 1 {
		t.Fatalf("Expected Users to increase by 1, got %d", got)
	}
}

func TestCreateUserWithWorkoutsDoesNotDuplicateParent(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	before, err := models.Users.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Users: %v", err)
	}

	if _, err := New().NewUserWithContext(ctx, UserMods.WithNewWorkouts(2)).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating User with Workouts: %v", err)
	}

	after, err := models.Users.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Users: %v", err)
	}

	if got := after - before; got != 1 {
		t.Fatalf("Expected Users to increase by 1, got %d", got)
	}
}

func TestCreateWorkoutComment(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	if _, err := New().NewWorkoutCommentWithContext(ctx).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating WorkoutComment: %v", err)
	}
}

func TestCreateWorkoutGroupExercise(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	if _, err := New().NewWorkoutGroupExerciseWithContext(ctx).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating WorkoutGroupExercise: %v", err)
	}
}

func TestCreateWorkoutGroupExerciseWithSetsDoesNotDuplicateParent(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	before, err := models.WorkoutGroupExercises.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting WorkoutGroupExercises: %v", err)
	}

	if _, err := New().NewWorkoutGroupExerciseWithContext(ctx, WorkoutGroupExerciseMods.WithNewSets(2)).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating WorkoutGroupExercise with Sets: %v", err)
	}

	after, err := models.WorkoutGroupExercises.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting WorkoutGroupExercises: %v", err)
	}

	if got := after - before; got != 1 {
		t.Fatalf("Expected WorkoutGroupExercises to increase by 1, got %d", got)
	}
}

func TestCreateWorkoutGroup(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	if _, err := New().NewWorkoutGroupWithContext(ctx).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating WorkoutGroup: %v", err)
	}
}

func TestCreateWorkoutGroupWithWorkoutGroupExercisesDoesNotDuplicateParent(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	before, err := models.WorkoutGroups.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting WorkoutGroups: %v", err)
	}

	if _, err := New().NewWorkoutGroupWithContext(ctx, WorkoutGroupMods.WithNewWorkoutGroupExercises(2)).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating WorkoutGroup with WorkoutGroupExercises: %v", err)
	}

	after, err := models.WorkoutGroups.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting WorkoutGroups: %v", err)
	}

	if got := after - before; got != 1 {
		t.Fatalf("Expected WorkoutGroups to increase by 1, got %d", got)
	}
}

func TestCreateWorkout(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	if _, err := New().NewWorkoutWithContext(ctx).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating Workout: %v", err)
	}
}

func TestCreateWorkoutWithSetsDoesNotDuplicateParent(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	before, err := models.Workouts.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Workouts: %v", err)
	}

	if _, err := New().NewWorkoutWithContext(ctx, WorkoutMods.WithNewSets(2)).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating Workout with Sets: %v", err)
	}

	after, err := models.Workouts.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Workouts: %v", err)
	}

	if got := after - before; got != 1 {
		t.Fatalf("Expected Workouts to increase by 1, got %d", got)
	}
}

func TestCreateWorkoutWithWorkoutCommentsDoesNotDuplicateParent(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	before, err := models.Workouts.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Workouts: %v", err)
	}

	if _, err := New().NewWorkoutWithContext(ctx, WorkoutMods.WithNewWorkoutComments(2)).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating Workout with WorkoutComments: %v", err)
	}

	after, err := models.Workouts.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Workouts: %v", err)
	}

	if got := after - before; got != 1 {
		t.Fatalf("Expected Workouts to increase by 1, got %d", got)
	}
}

func TestCreateWorkoutWithWorkoutGroupsDoesNotDuplicateParent(t *testing.T) {
	if testDB == nil {
		t.Skip("skipping test, no DSN provided")
	}

	ctx, cancel := context.WithCancel(t.Context())
	t.Cleanup(cancel)

	tx, err := testDB.Begin(ctx)
	if err != nil {
		t.Fatalf("Error starting transaction: %v", err)
	}

	defer func() {
		if err := tx.Rollback(ctx); err != nil {
			t.Fatalf("Error rolling back transaction: %v", err)
		}
	}()

	before, err := models.Workouts.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Workouts: %v", err)
	}

	if _, err := New().NewWorkoutWithContext(ctx, WorkoutMods.WithNewWorkoutGroups(2)).Create(ctx, tx); err != nil {
		t.Fatalf("Error creating Workout with WorkoutGroups: %v", err)
	}

	after, err := models.Workouts.Query().Count(ctx, tx)
	if err != nil {
		t.Fatalf("Error counting Workouts: %v", err)
	}

	if got := after - before; got != 1 {
		t.Fatalf("Expected Workouts to increase by 1, got %d", got)
	}
}
