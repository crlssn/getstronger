// Code generated by BobGen psql v0.50.0. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"encoding/json"
	"fmt"
	"io"
	"strconv"
	"strings"
	"time"

	"github.com/aarondl/opt/null"
	"github.com/aarondl/opt/omit"
	"github.com/aarondl/opt/omitnull"
	"github.com/stephenafamo/bob"
	"github.com/stephenafamo/bob/dialect/psql"
	"github.com/stephenafamo/bob/dialect/psql/dialect"
	"github.com/stephenafamo/bob/dialect/psql/dm"
	"github.com/stephenafamo/bob/dialect/psql/sm"
	"github.com/stephenafamo/bob/dialect/psql/um"
	"github.com/stephenafamo/bob/expr"
	"github.com/stephenafamo/bob/mods"
	"github.com/stephenafamo/bob/orm"
	"github.com/stephenafamo/bob/types"
	"github.com/stephenafamo/bob/types/pgtypes"
	"github.com/stephenafamo/scan"
)

// Routine is an object representing the database table.
type Routine struct {
	ID            string                      `db:"id,pk" `
	UserID        string                      `db:"user_id" `
	Title         string                      `db:"title" `
	CreatedAt     time.Time                   `db:"created_at" `
	DeletedAt     null.Val[time.Time]         `db:"deleted_at" `
	ExerciseOrder types.JSON[json.RawMessage] `db:"exercise_order" `

	R routineR `db:"-" `

	C routineC `db:"-" `
}

// RoutineSlice is an alias for a slice of pointers to Routine.
// This should almost always be used instead of []*Routine.
type RoutineSlice []*Routine

// Routines contains methods to work with the routines table
var Routines = psql.NewTablex[*Routine, RoutineSlice, *RoutineSetter]("", "routines", buildRoutineColumns("routines"), routineScanMapper)

// RoutinesQuery is a query on the routines table
type RoutinesQuery = *psql.ViewQuery[*Routine, RoutineSlice]

// routineR is where relationships are stored.
type routineR struct {
	Exercises    ExerciseSlice    // exercises_routines.routine_exercises_exercise_id_fkeyexercises_routines.routine_exercises_routine_id_fkey
	PlanRoutines PlanRoutineSlice // plan_routines.plan_routines_routine_id_fkey
	User         *User            // routines.routines_user_id_fkey
	Workouts     WorkoutSlice     // workouts.workouts_routine_id_fkey
	// Loaded reports whether each relationship has been loaded.
	// A relationship's bool is set by Load*, Preload, ThenLoad, factory builds,
	// and to-one Attach/Insert operations. To-many Attach/Insert operations leave it unchanged.
	Loaded routineRLoaded `db:"-" `
}

// routineRLoaded tracks which relationships on Routine have been loaded.
type routineRLoaded struct {
	Exercises    bool // exercises_routines.routine_exercises_exercise_id_fkeyexercises_routines.routine_exercises_routine_id_fkey
	PlanRoutines bool // plan_routines.plan_routines_routine_id_fkey
	User         bool // routines.routines_user_id_fkey
	Workouts     bool // workouts.workouts_routine_id_fkey
}

func buildRoutineColumns(tableName string) routineColumns {
	columnsExpr := expr.NewColumnsExpr(
		"id", "user_id", "title", "created_at", "deleted_at", "exercise_order",
	)

	if tableName != "" {
		columnsExpr = columnsExpr.WithParent(tableName)
	}

	return routineColumns{
		ColumnsExpr:   columnsExpr,
		tableAlias:    tableName,
		ID:            buildRoutineColumn(tableName, "id"),
		UserID:        buildRoutineColumn(tableName, "user_id"),
		Title:         buildRoutineColumn(tableName, "title"),
		CreatedAt:     buildRoutineColumn(tableName, "created_at"),
		DeletedAt:     buildRoutineColumn(tableName, "deleted_at"),
		ExerciseOrder: buildRoutineColumn(tableName, "exercise_order"),
	}
}

type routineColumns struct {
	expr.ColumnsExpr
	tableAlias    string
	ID            routineColumn
	UserID        routineColumn
	Title         routineColumn
	CreatedAt     routineColumn
	DeletedAt     routineColumn
	ExerciseOrder routineColumn
}

// Alias returns the current table alias for the columns set.
func (c routineColumns) Alias() string {
	return c.tableAlias
}

// AliasedAs returns a copy of the columns set qualified by tableName.
func (routineColumns) AliasedAs(tableName string) routineColumns {
	return buildRoutineColumns(tableName)
}

// Unqualified returns a copy of the columns set without table qualification.
func (c routineColumns) Unqualified() routineColumns {
	return buildRoutineColumns("")
}

func buildRoutineColumn(alias, name string) routineColumn {
	return routineColumn{
		Expression: psql.Quote(alias, name),
		alias:      alias,
		name:       name,
	}
}

type routineColumn struct {
	psql.Expression
	alias string
	name  string
}

// Name returns the unqualified column name.
func (c routineColumn) Name() string {
	return c.name
}

// ShouldOmitParens prevents automatic parenthesis wrapping in expression builders.
func (c routineColumn) ShouldOmitParens() bool {
	return true
}

// RoutineSetter is used for insert/upsert/update operations
// All values are optional, and do not have to be set
// Generated columns are not included
type RoutineSetter struct {
	ID            omit.Val[string]                      `db:"id,pk" `
	UserID        omit.Val[string]                      `db:"user_id" `
	Title         omit.Val[string]                      `db:"title" `
	CreatedAt     omit.Val[time.Time]                   `db:"created_at" `
	DeletedAt     omitnull.Val[time.Time]               `db:"deleted_at" `
	ExerciseOrder omit.Val[types.JSON[json.RawMessage]] `db:"exercise_order" `
}

func (s RoutineSetter) SetColumns() []string {
	vals := make([]string, 0, 6)
	if s.ID.IsValue() {
		vals = append(vals, "id")
	}
	if s.UserID.IsValue() {
		vals = append(vals, "user_id")
	}
	if s.Title.IsValue() {
		vals = append(vals, "title")
	}
	if s.CreatedAt.IsValue() {
		vals = append(vals, "created_at")
	}
	if s.DeletedAt.IsValue() || s.DeletedAt.IsNull() {
		vals = append(vals, "deleted_at")
	}
	if s.ExerciseOrder.IsValue() {
		vals = append(vals, "exercise_order")
	}
	return vals
}

func (s RoutineSetter) Overwrite(t *Routine) {
	if s.ID.IsValue() {
		t.ID = s.ID.MustGet()
	}
	if s.UserID.IsValue() {
		t.UserID = s.UserID.MustGet()
	}
	if s.Title.IsValue() {
		t.Title = s.Title.MustGet()
	}
	if s.CreatedAt.IsValue() {
		t.CreatedAt = s.CreatedAt.MustGet()
	}
	if s.DeletedAt.IsValue() || s.DeletedAt.IsNull() {
		t.DeletedAt = s.DeletedAt.MustGetNull()
	}
	if s.ExerciseOrder.IsValue() {
		t.ExerciseOrder = s.ExerciseOrder.MustGet()
	}
}

func (s *RoutineSetter) Apply(q *dialect.InsertQuery) {
	q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
		return Routines.BeforeInsertHooks.RunHooks(ctx, exec, s)
	})

	q.AppendValues(
		bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
			if s.ID.IsUnset() {
				return psql.Raw("DEFAULT").WriteSQL(ctx, w, d, start)
			}
			return psql.Arg(s.ID.MustGet()).WriteSQL(ctx, w, d, start)
		}), bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
			if s.UserID.IsUnset() {
				return psql.Raw("DEFAULT").WriteSQL(ctx, w, d, start)
			}
			return psql.Arg(s.UserID.MustGet()).WriteSQL(ctx, w, d, start)
		}), bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
			if s.Title.IsUnset() {
				return psql.Raw("DEFAULT").WriteSQL(ctx, w, d, start)
			}
			return psql.Arg(s.Title.MustGet()).WriteSQL(ctx, w, d, start)
		}), bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
			if s.CreatedAt.IsUnset() {
				return psql.Raw("DEFAULT").WriteSQL(ctx, w, d, start)
			}
			return psql.Arg(s.CreatedAt.MustGet()).WriteSQL(ctx, w, d, start)
		}), bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
			if s.DeletedAt.IsUnset() {
				return psql.Raw("DEFAULT").WriteSQL(ctx, w, d, start)
			}
			return psql.Arg(s.DeletedAt.MustGetNull()).WriteSQL(ctx, w, d, start)
		}), bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
			if s.ExerciseOrder.IsUnset() {
				return psql.Raw("DEFAULT").WriteSQL(ctx, w, d, start)
			}
			return psql.Arg(s.ExerciseOrder.MustGet()).WriteSQL(ctx, w, d, start)
		}))
}

func (s RoutineSetter) UpdateMod() bob.Mod[*dialect.UpdateQuery] {
	return um.Set(s.Expressions()...)
}

func (s RoutineSetter) Expressions(prefix ...string) []bob.Expression {
	exprs := make([]bob.Expression, 0, 6)

	if s.ID.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "id")...),
			psql.Arg(s.ID),
		}})
	}

	if s.UserID.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "user_id")...),
			psql.Arg(s.UserID),
		}})
	}

	if s.Title.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "title")...),
			psql.Arg(s.Title),
		}})
	}

	if s.CreatedAt.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "created_at")...),
			psql.Arg(s.CreatedAt),
		}})
	}

	if s.DeletedAt.IsValue() || s.DeletedAt.IsNull() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "deleted_at")...),
			psql.Arg(s.DeletedAt),
		}})
	}

	if s.ExerciseOrder.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "exercise_order")...),
			psql.Arg(s.ExerciseOrder),
		}})
	}

	return exprs
}

// scan.Mapper[*T] without reflection: maps columns to fields by index.
func routineScanMapper(ctx context.Context, cols []string) (scan.BeforeFunc, func(any) (*Routine, error)) {
	// resolve the column positions once per query, not per row
	type target struct {
		idx int
		dst func(o *Routine) any
	}
	targets := make([]target, 0, 6)
	for i, col := range cols {
		switch col {
		case "id":
			targets = append(targets, target{i, func(o *Routine) any { return &o.ID }})
		case "user_id":
			targets = append(targets, target{i, func(o *Routine) any { return &o.UserID }})
		case "title":
			targets = append(targets, target{i, func(o *Routine) any { return &o.Title }})
		case "created_at":
			targets = append(targets, target{i, func(o *Routine) any { return &o.CreatedAt }})
		case "deleted_at":
			targets = append(targets, target{i, func(o *Routine) any { return &o.DeletedAt }})
		case "exercise_order":
			targets = append(targets, target{i, func(o *Routine) any { return &o.ExerciseOrder }})
		}
	}

	return func(row *scan.Row) (any, error) {
			o := new(Routine)
			for _, t := range targets {
				row.ScheduleScanByIndex(t.idx, t.dst(o))
			}
			return o, nil
		}, func(v any) (*Routine, error) {
			return v.(*Routine), nil
		}
}

// FindRoutine retrieves a single record by primary key
// If cols is empty Find will return all columns.
func FindRoutine(ctx context.Context, exec bob.Executor, IDPK string, cols ...string) (*Routine, error) {
	if len(cols) == 0 {
		return Routines.Query(
			sm.Where(Routines.Columns.ID.EQ(psql.Arg(IDPK))),
		).One(ctx, exec)
	}

	return Routines.Query(
		sm.Where(Routines.Columns.ID.EQ(psql.Arg(IDPK))),
		sm.Columns(Routines.Columns.Only(cols...)),
	).One(ctx, exec)
}

// RoutineExists checks the presence of a single record by primary key
func RoutineExists(ctx context.Context, exec bob.Executor, IDPK string) (bool, error) {
	return Routines.Query(
		sm.Where(Routines.Columns.ID.EQ(psql.Arg(IDPK))),
	).Exists(ctx, exec)
}

// AfterQueryHook is called after Routine is retrieved from the database
func (o *Routine) AfterQueryHook(ctx context.Context, exec bob.Executor, queryType bob.QueryType) error {
	var err error

	switch queryType {
	case bob.QueryTypeSelect:
		ctx, err = Routines.AfterSelectHooks.RunHooks(ctx, exec, RoutineSlice{o})
	case bob.QueryTypeInsert:
		ctx, err = Routines.AfterInsertHooks.RunHooks(ctx, exec, RoutineSlice{o})
	case bob.QueryTypeUpdate:
		ctx, err = Routines.AfterUpdateHooks.RunHooks(ctx, exec, RoutineSlice{o})
	case bob.QueryTypeDelete:
		ctx, err = Routines.AfterDeleteHooks.RunHooks(ctx, exec, RoutineSlice{o})
	case bob.QueryTypeMerge:
		ctx, err = Routines.AfterMergeHooks.RunHooks(ctx, exec, RoutineSlice{o})
	}

	return err
}

// primaryKeyVals returns the primary key values of the Routine
func (o *Routine) primaryKeyVals() bob.Expression {
	return psql.Arg(o.ID)
}

func (o *Routine) pkEQ() dialect.Expression {
	return psql.Quote("routines", "id").EQ(bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
		return o.primaryKeyVals().WriteSQL(ctx, w, d, start)
	}))
}

// Update uses an executor to update the Routine
func (o *Routine) Update(ctx context.Context, exec bob.Executor, s *RoutineSetter) error {
	v, err := Routines.Update(s.UpdateMod(), um.Where(o.pkEQ())).One(ctx, exec)
	if err != nil {
		return err
	}

	o.R = v.R
	*o = *v

	return nil
}

// Delete deletes a single Routine record with an executor
func (o *Routine) Delete(ctx context.Context, exec bob.Executor) error {
	_, err := Routines.Delete(dm.Where(o.pkEQ())).Exec(ctx, exec)
	return err
}

// Reload refreshes the Routine using the executor
func (o *Routine) Reload(ctx context.Context, exec bob.Executor) error {
	o2, err := Routines.Query(
		sm.Where(Routines.Columns.ID.EQ(psql.Arg(o.ID))),
	).One(ctx, exec)
	if err != nil {
		return err
	}
	o2.R = o.R
	*o = *o2

	return nil
}

// AfterQueryHook is called after RoutineSlice is retrieved from the database
func (o RoutineSlice) AfterQueryHook(ctx context.Context, exec bob.Executor, queryType bob.QueryType) error {
	var err error

	switch queryType {
	case bob.QueryTypeSelect:
		ctx, err = Routines.AfterSelectHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeInsert:
		ctx, err = Routines.AfterInsertHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeUpdate:
		ctx, err = Routines.AfterUpdateHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeDelete:
		ctx, err = Routines.AfterDeleteHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeMerge:
		ctx, err = Routines.AfterMergeHooks.RunHooks(ctx, exec, o)
	}

	return err
}

func (o RoutineSlice) pkIN() dialect.Expression {
	if len(o) == 0 {
		return psql.Raw("NULL")
	}

	return psql.Quote("routines", "id").In(bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
		pkPairs := make([]bob.Expression, len(o))
		for i, row := range o {
			pkPairs[i] = row.primaryKeyVals()
		}
		return bob.ExpressSlice(ctx, w, d, start, pkPairs, "", ", ", "")
	}))
}

// copyMatchingRows finds models in the given slice that have the same primary key
// then it first copies the existing relationships from the old model to the new model
// and then replaces the old model in the slice with the new model
func (o RoutineSlice) copyMatchingRows(from ...*Routine) {
	fromByPK := make(map[string]*Routine, len(from))
	for _, new := range from {
		// keep the first row for each key, like the nested loop did
		if _, ok := fromByPK[new.ID]; !ok {
			fromByPK[new.ID] = new
		}
	}

	for i, old := range o {
		new, ok := fromByPK[old.ID]
		if !ok {
			continue
		}
		new.R = old.R
		o[i] = new
	}
}

// UpdateMod modifies an update query with "WHERE primary_key IN (o...)"
func (o RoutineSlice) UpdateMod() bob.Mod[*dialect.UpdateQuery] {
	return bob.ModFunc[*dialect.UpdateQuery](func(q *dialect.UpdateQuery) {
		q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
			return Routines.BeforeUpdateHooks.RunHooks(ctx, exec, o)
		})

		q.AppendLoader(bob.LoaderFunc(func(ctx context.Context, exec bob.Executor, retrieved any) error {
			var err error
			switch retrieved := retrieved.(type) {
			case *Routine:
				o.copyMatchingRows(retrieved)
			case []*Routine:
				o.copyMatchingRows(retrieved...)
			case RoutineSlice:
				o.copyMatchingRows(retrieved...)
			default:
				// If the retrieved value is not a Routine or a slice of Routine
				// then run the AfterUpdateHooks on the slice
				_, err = Routines.AfterUpdateHooks.RunHooks(ctx, exec, o)
			}

			return err
		}))

		q.AppendWhere(o.pkIN())
	})
}

// DeleteMod modifies an delete query with "WHERE primary_key IN (o...)"
func (o RoutineSlice) DeleteMod() bob.Mod[*dialect.DeleteQuery] {
	return bob.ModFunc[*dialect.DeleteQuery](func(q *dialect.DeleteQuery) {
		q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
			return Routines.BeforeDeleteHooks.RunHooks(ctx, exec, o)
		})

		q.AppendLoader(bob.LoaderFunc(func(ctx context.Context, exec bob.Executor, retrieved any) error {
			var err error
			switch retrieved := retrieved.(type) {
			case *Routine:
				o.copyMatchingRows(retrieved)
			case []*Routine:
				o.copyMatchingRows(retrieved...)
			case RoutineSlice:
				o.copyMatchingRows(retrieved...)
			default:
				// If the retrieved value is not a Routine or a slice of Routine
				// then run the AfterDeleteHooks on the slice
				_, err = Routines.AfterDeleteHooks.RunHooks(ctx, exec, o)
			}

			return err
		}))

		q.AppendWhere(o.pkIN())
	})
}

// MergeMod modifies a merge query to run BeforeMergeHooks and AfterMergeHooks
// and updates the slice with the returned rows.
func (o RoutineSlice) MergeMod() bob.Mod[*dialect.MergeQuery] {
	return bob.ModFunc[*dialect.MergeQuery](func(q *dialect.MergeQuery) {
		q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
			return Routines.BeforeMergeHooks.RunHooks(ctx, exec, o)
		})

		q.AppendLoader(bob.LoaderFunc(func(ctx context.Context, exec bob.Executor, retrieved any) error {
			var err error
			switch retrieved := retrieved.(type) {
			case *Routine:
				o.copyMatchingRows(retrieved)
			case []*Routine:
				o.copyMatchingRows(retrieved...)
			case RoutineSlice:
				o.copyMatchingRows(retrieved...)
			default:
				// If the retrieved value is not a Routine or a slice of Routine
				// then run the AfterMergeHooks on the slice
				_, err = Routines.AfterMergeHooks.RunHooks(ctx, exec, o)
			}

			return err
		}))
	})
}

func (o RoutineSlice) UpdateAll(ctx context.Context, exec bob.Executor, vals RoutineSetter) error {
	if len(o) == 0 {
		return nil
	}

	_, err := Routines.Update(vals.UpdateMod(), o.UpdateMod()).All(ctx, exec)
	return err
}

func (o RoutineSlice) DeleteAll(ctx context.Context, exec bob.Executor) error {
	if len(o) == 0 {
		return nil
	}

	_, err := Routines.Delete(o.DeleteMod()).Exec(ctx, exec)
	return err
}

func (o RoutineSlice) ReloadAll(ctx context.Context, exec bob.Executor) error {
	if len(o) == 0 {
		return nil
	}

	o2, err := Routines.Query(sm.Where(o.pkIN())).All(ctx, exec)
	if err != nil {
		return err
	}

	o.copyMatchingRows(o2...)

	return nil
}

// Exercises starts a query for related objects on exercises
func (o *Routine) Exercises(mods ...bob.Mod[*dialect.SelectQuery]) ExercisesQuery {
	return Exercises.Query(append(mods,
		sm.InnerJoin(ExercisesRoutines.NameAsExpr()).On(
			Exercises.Columns.ID.EQ(ExercisesRoutines.Columns.ExerciseID)),
		sm.Where(ExercisesRoutines.Columns.RoutineID.EQ(psql.Arg(o.ID))),
	)...)
}

func (os RoutineSlice) Exercises(mods ...bob.Mod[*dialect.SelectQuery]) ExercisesQuery {
	pkID := make(pgtypes.Array[string], 0, len(os))

	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}
	PKArgExpr := psql.Any(psql.Cast(psql.Arg(pkID), "uuid[]"))

	return Exercises.Query(append(mods,
		sm.InnerJoin(ExercisesRoutines.NameAsExpr()).On(
			Exercises.Columns.ID.EQ(ExercisesRoutines.Columns.ExerciseID),
		),
		sm.Where(ExercisesRoutines.Columns.RoutineID.EQ(PKArgExpr)),
	)...)
}

// PlanRoutines starts a query for related objects on plan_routines
func (o *Routine) PlanRoutines(mods ...bob.Mod[*dialect.SelectQuery]) PlanRoutinesQuery {
	return PlanRoutines.Query(append(mods,
		sm.Where(PlanRoutines.Columns.RoutineID.EQ(psql.Arg(o.ID))),
	)...)
}

func (os RoutineSlice) PlanRoutines(mods ...bob.Mod[*dialect.SelectQuery]) PlanRoutinesQuery {
	pkID := make(pgtypes.Array[string], 0, len(os))

	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}
	PKArgExpr := psql.Any(psql.Cast(psql.Arg(pkID), "uuid[]"))

	return PlanRoutines.Query(append(mods,
		sm.Where(PlanRoutines.Columns.RoutineID.EQ(PKArgExpr)),
	)...)
}

// User starts a query for related objects on users
func (o *Routine) User(mods ...bob.Mod[*dialect.SelectQuery]) UsersQuery {
	return Users.Query(append(mods,
		sm.Where(Users.Columns.ID.EQ(psql.Arg(o.UserID))),
	)...)
}

func (os RoutineSlice) User(mods ...bob.Mod[*dialect.SelectQuery]) UsersQuery {
	pkUserID := make(pgtypes.Array[string], 0, len(os))

	// the array is only a filter (semi-join), so duplicate keys can be
	// dropped before they are sent over the wire
	seenUserID := make(map[string]struct{}, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		if _, ok := seenUserID[o.UserID]; ok {
			continue
		}
		seenUserID[o.UserID] = struct{}{}
		pkUserID = append(pkUserID, o.UserID)
	}
	PKArgExpr := psql.Any(psql.Cast(psql.Arg(pkUserID), "uuid[]"))

	return Users.Query(append(mods,
		sm.Where(Users.Columns.ID.EQ(PKArgExpr)),
	)...)
}

// Workouts starts a query for related objects on workouts
func (o *Routine) Workouts(mods ...bob.Mod[*dialect.SelectQuery]) WorkoutsQuery {
	return Workouts.Query(append(mods,
		sm.Where(Workouts.Columns.RoutineID.EQ(psql.Arg(o.ID))),
	)...)
}

func (os RoutineSlice) Workouts(mods ...bob.Mod[*dialect.SelectQuery]) WorkoutsQuery {
	pkID := make(pgtypes.Array[string], 0, len(os))

	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}
	PKArgExpr := psql.Any(psql.Cast(psql.Arg(pkID), "uuid[]"))

	return Workouts.Query(append(mods,
		sm.Where(Workouts.Columns.RoutineID.EQ(PKArgExpr)),
	)...)
}

func attachRoutineExercises0(ctx context.Context, exec bob.Executor, count int, routine0 *Routine, exercises2 ExerciseSlice) (ExercisesRoutineSlice, error) {
	setters := make([]*ExercisesRoutineSetter, count)
	for i := range count {
		setters[i] = &ExercisesRoutineSetter{
			RoutineID:  omit.From(routine0.ID),
			ExerciseID: omit.From(exercises2[i].ID),
		}
	}

	exercisesRoutines1, err := ExercisesRoutines.Insert(bob.ToMods(setters...)).All(ctx, exec)
	if err != nil {
		return nil, fmt.Errorf("attachRoutineExercises0: %w", err)
	}

	return exercisesRoutines1, nil
}

func (routine0 *Routine) InsertExercises(ctx context.Context, exec bob.Executor, related ...*ExerciseSetter) error {
	if len(related) == 0 {
		return nil
	}

	var err error

	inserted, err := Exercises.Insert(bob.ToMods(related...)).All(ctx, exec)
	if err != nil {
		return fmt.Errorf("inserting related objects: %w", err)
	}
	exercises2 := ExerciseSlice(inserted)

	_, err = attachRoutineExercises0(ctx, exec, len(related), routine0, exercises2)
	if err != nil {
		return err
	}

	routine0.R.Exercises = append(routine0.R.Exercises, exercises2...)

	for _, rel := range exercises2 {
		rel.R.Routines = append(rel.R.Routines, routine0)
	}
	return nil
}

func (routine0 *Routine) AttachExercises(ctx context.Context, exec bob.Executor, related ...*Exercise) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	exercises2 := ExerciseSlice(related)

	_, err = attachRoutineExercises0(ctx, exec, len(related), routine0, exercises2)
	if err != nil {
		return err
	}

	routine0.R.Exercises = append(routine0.R.Exercises, exercises2...)

	for _, rel := range related {
		rel.R.Routines = append(rel.R.Routines, routine0)
	}

	return nil
}

func insertRoutinePlanRoutines0(ctx context.Context, exec bob.Executor, planRoutines1 []*PlanRoutineSetter, routine0 *Routine) (PlanRoutineSlice, error) {
	for i := range planRoutines1 {
		planRoutines1[i].RoutineID = omit.From(routine0.ID)
	}

	ret, err := PlanRoutines.Insert(bob.ToMods(planRoutines1...)).All(ctx, exec)
	if err != nil {
		return ret, fmt.Errorf("insertRoutinePlanRoutines0: %w", err)
	}

	return ret, nil
}

func attachRoutinePlanRoutines0(ctx context.Context, exec bob.Executor, count int, planRoutines1 PlanRoutineSlice, routine0 *Routine) (PlanRoutineSlice, error) {
	setter := &PlanRoutineSetter{
		RoutineID: omit.From(routine0.ID),
	}

	err := planRoutines1.UpdateAll(ctx, exec, *setter)
	if err != nil {
		return nil, fmt.Errorf("attachRoutinePlanRoutines0: %w", err)
	}

	return planRoutines1, nil
}

func (routine0 *Routine) InsertPlanRoutines(ctx context.Context, exec bob.Executor, related ...*PlanRoutineSetter) error {
	if len(related) == 0 {
		return nil
	}

	var err error

	planRoutines1, err := insertRoutinePlanRoutines0(ctx, exec, related, routine0)
	if err != nil {
		return err
	}

	routine0.R.PlanRoutines = append(routine0.R.PlanRoutines, planRoutines1...)

	for _, rel := range planRoutines1 {
		rel.R.Routine = routine0
		rel.R.Loaded.Routine = true
	}
	return nil
}

func (routine0 *Routine) AttachPlanRoutines(ctx context.Context, exec bob.Executor, related ...*PlanRoutine) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	planRoutines1 := PlanRoutineSlice(related)

	_, err = attachRoutinePlanRoutines0(ctx, exec, len(related), planRoutines1, routine0)
	if err != nil {
		return err
	}

	routine0.R.PlanRoutines = append(routine0.R.PlanRoutines, planRoutines1...)

	for _, rel := range related {
		rel.R.Routine = routine0
		rel.R.Loaded.Routine = true
	}

	return nil
}

func attachRoutineUser0(ctx context.Context, exec bob.Executor, count int, routine0 *Routine, user1 *User) (*Routine, error) {
	setter := &RoutineSetter{
		UserID: omit.From(user1.ID),
	}

	err := routine0.Update(ctx, exec, setter)
	if err != nil {
		return nil, fmt.Errorf("attachRoutineUser0: %w", err)
	}

	return routine0, nil
}

func (routine0 *Routine) InsertUser(ctx context.Context, exec bob.Executor, related *UserSetter) error {
	var err error

	user1, err := Users.Insert(related).One(ctx, exec)
	if err != nil {
		return fmt.Errorf("inserting related objects: %w", err)
	}

	_, err = attachRoutineUser0(ctx, exec, 1, routine0, user1)
	if err != nil {
		return err
	}

	routine0.R.User = user1
	routine0.R.Loaded.User = true

	user1.R.Routines = append(user1.R.Routines, routine0)

	return nil
}

func (routine0 *Routine) AttachUser(ctx context.Context, exec bob.Executor, user1 *User) error {
	var err error

	_, err = attachRoutineUser0(ctx, exec, 1, routine0, user1)
	if err != nil {
		return err
	}

	routine0.R.User = user1
	routine0.R.Loaded.User = true

	user1.R.Routines = append(user1.R.Routines, routine0)

	return nil
}

func insertRoutineWorkouts0(ctx context.Context, exec bob.Executor, workouts1 []*WorkoutSetter, routine0 *Routine) (WorkoutSlice, error) {
	for i := range workouts1 {
		workouts1[i].RoutineID = omitnull.From(routine0.ID)
	}

	ret, err := Workouts.Insert(bob.ToMods(workouts1...)).All(ctx, exec)
	if err != nil {
		return ret, fmt.Errorf("insertRoutineWorkouts0: %w", err)
	}

	return ret, nil
}

func attachRoutineWorkouts0(ctx context.Context, exec bob.Executor, count int, workouts1 WorkoutSlice, routine0 *Routine) (WorkoutSlice, error) {
	setter := &WorkoutSetter{
		RoutineID: omitnull.From(routine0.ID),
	}

	err := workouts1.UpdateAll(ctx, exec, *setter)
	if err != nil {
		return nil, fmt.Errorf("attachRoutineWorkouts0: %w", err)
	}

	return workouts1, nil
}

func (routine0 *Routine) InsertWorkouts(ctx context.Context, exec bob.Executor, related ...*WorkoutSetter) error {
	if len(related) == 0 {
		return nil
	}

	var err error

	workouts1, err := insertRoutineWorkouts0(ctx, exec, related, routine0)
	if err != nil {
		return err
	}

	routine0.R.Workouts = append(routine0.R.Workouts, workouts1...)

	for _, rel := range workouts1 {
		rel.R.Routine = routine0
		rel.R.Loaded.Routine = true
	}
	return nil
}

func (routine0 *Routine) AttachWorkouts(ctx context.Context, exec bob.Executor, related ...*Workout) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	workouts1 := WorkoutSlice(related)

	_, err = attachRoutineWorkouts0(ctx, exec, len(related), workouts1, routine0)
	if err != nil {
		return err
	}

	routine0.R.Workouts = append(routine0.R.Workouts, workouts1...)

	for _, rel := range related {
		rel.R.Routine = routine0
		rel.R.Loaded.Routine = true
	}

	return nil
}

type routineWhere[Q psql.Filterable] struct {
	cols          routineColumns
	ID            psql.WhereMod[Q, string]
	UserID        psql.WhereMod[Q, string]
	Title         psql.WhereMod[Q, string]
	CreatedAt     psql.WhereMod[Q, time.Time]
	DeletedAt     psql.WhereNullMod[Q, time.Time]
	ExerciseOrder psql.WhereMod[Q, types.JSON[json.RawMessage]]
	R             routineWhereR[Q]
}

func (routineWhere[Q]) AliasedAs(alias string) routineWhere[Q] {
	return buildRoutineWhere[Q](buildRoutineColumns(alias))
}

func buildRoutineWhere[Q psql.Filterable](cols routineColumns) routineWhere[Q] {
	return routineWhere[Q]{
		cols:          cols,
		ID:            psql.Where[Q, string](cols.ID.Expression),
		UserID:        psql.Where[Q, string](cols.UserID.Expression),
		Title:         psql.Where[Q, string](cols.Title.Expression),
		CreatedAt:     psql.Where[Q, time.Time](cols.CreatedAt.Expression),
		DeletedAt:     psql.WhereNull[Q, time.Time](cols.DeletedAt.Expression),
		ExerciseOrder: psql.Where[Q, types.JSON[json.RawMessage]](cols.ExerciseOrder.Expression),
		R:             routineWhereR[Q]{cols: cols},
	}
}

// routineWhereR holds the relationship-based filters of
// routineWhere under the R namespace — mirroring the model's
// R struct — so they cannot collide with column-based filter fields.
type routineWhereR[Q psql.Filterable] struct {
	cols routineColumns
}

// HasPlanRoutines filters parents that have a matching PlanRoutines using a
// correlated EXISTS subquery (semi-join). Unlike an INNER JOIN it does not
// multiply parent rows, so no DISTINCT is needed. The optional filters are
// applied to the subquery (i.e. to PlanRoutines).
func (w routineWhereR[Q]) HasPlanRoutines(filters ...bob.Mod[*dialect.SelectQuery]) mods.Where[Q] {
	q := psql.Select(
		sm.Columns(psql.Raw("1")),
		sm.From(PlanRoutines.NameExpr()),
		sm.Where(PlanRoutines.Columns.RoutineID.EQ(w.cols.ID)),
	)
	q.Apply(filters...)
	return mods.Where[Q]{E: psql.Exists(q)}
}

// HasUser filters parents that have a matching User using a
// correlated EXISTS subquery (semi-join). Unlike an INNER JOIN it does not
// multiply parent rows, so no DISTINCT is needed. The optional filters are
// applied to the subquery (i.e. to Users).
func (w routineWhereR[Q]) HasUser(filters ...bob.Mod[*dialect.SelectQuery]) mods.Where[Q] {
	q := psql.Select(
		sm.Columns(psql.Raw("1")),
		sm.From(Users.NameExpr()),
		sm.Where(Users.Columns.ID.EQ(w.cols.UserID)),
	)
	q.Apply(filters...)
	return mods.Where[Q]{E: psql.Exists(q)}
}

// HasWorkouts filters parents that have a matching Workouts using a
// correlated EXISTS subquery (semi-join). Unlike an INNER JOIN it does not
// multiply parent rows, so no DISTINCT is needed. The optional filters are
// applied to the subquery (i.e. to Workouts).
func (w routineWhereR[Q]) HasWorkouts(filters ...bob.Mod[*dialect.SelectQuery]) mods.Where[Q] {
	q := psql.Select(
		sm.Columns(psql.Raw("1")),
		sm.From(Workouts.NameExpr()),
		sm.Where(Workouts.Columns.RoutineID.EQ(w.cols.ID)),
	)
	q.Apply(filters...)
	return mods.Where[Q]{E: psql.Exists(q)}
}

// HasExercises filters parents that have a matching Exercises (a
// many-to-many relationship) using a correlated EXISTS subquery instead of
// INNER JOINs, so the parent rows are not multiplied. The optional filters are
// applied to the subquery (i.e. to Exercises).
func (w routineWhereR[Q]) HasExercises(filters ...bob.Mod[*dialect.SelectQuery]) mods.Where[Q] {
	q := psql.Select(
		sm.Columns(psql.Raw("1")),
		sm.From(ExercisesRoutines.NameExpr()),
		sm.InnerJoin(Exercises.NameExpr()).On(
			Exercises.Columns.ID.EQ(ExercisesRoutines.Columns.ExerciseID),
		),
		sm.Where(ExercisesRoutines.Columns.RoutineID.EQ(w.cols.ID)),
	)
	q.Apply(filters...)
	return mods.Where[Q]{E: psql.Exists(q)}
}

// NULL-tolerant scan buffer for routineScanMapperNullable:
// on a LEFT JOIN miss every column comes back NULL, so each field uses the
// nullable version of the column type even when the column itself is NOT NULL.
type routinePreloadBuf struct {
	ID            null.Val[string]
	UserID        null.Val[string]
	Title         null.Val[string]
	CreatedAt     null.Val[time.Time]
	DeletedAt     null.Val[time.Time]
	ExerciseOrder null.Val[types.JSON[json.RawMessage]]
}

// routineScanMapperNullable maps the preloaded routine
// columns (prefixed with the runtime join alias) without reflection, while
// keeping the LEFT JOIN semantics of the reflection-based mapper: a row whose
// prefixed columns are all NULL yields nil (no child), and NULL values never
// error, they just leave the zero value in the field.
func routineScanMapperNullable(prefix string) scan.Mapper[*Routine] {
	return func(ctx context.Context, cols []string) (scan.BeforeFunc, func(any) (*Routine, error)) {
		// resolve the column names once per query, not per row
		type target struct {
			idx int
			dst func(b *routinePreloadBuf) any
		}
		targets := make([]target, 0, 6)
		for i, col := range cols {
			name, ok := strings.CutPrefix(col, prefix)
			if !ok {
				continue
			}
			switch name {
			case "id":
				targets = append(targets, target{i, func(b *routinePreloadBuf) any { return &b.ID }})
			case "user_id":
				targets = append(targets, target{i, func(b *routinePreloadBuf) any { return &b.UserID }})
			case "title":
				targets = append(targets, target{i, func(b *routinePreloadBuf) any { return &b.Title }})
			case "created_at":
				targets = append(targets, target{i, func(b *routinePreloadBuf) any { return &b.CreatedAt }})
			case "deleted_at":
				targets = append(targets, target{i, func(b *routinePreloadBuf) any { return &b.DeletedAt }})
			case "exercise_order":
				targets = append(targets, target{i, func(b *routinePreloadBuf) any { return &b.ExerciseOrder }})
			}
		}

		// One scratch buffer reused for every row: before -> scan -> after runs
		// synchronously per row, the scheduled columns are fully overwritten each
		// row, and unselected columns stay zero, so a single buffer preserves the
		// per-row NULL semantics without allocating a buffer per parent row. The
		// returned child (o below) is always freshly allocated, so buf never
		// escapes past the after func.
		buf := new(routinePreloadBuf)
		return func(row *scan.Row) (any, error) {
				for _, t := range targets {
					row.ScheduleScanByIndex(t.idx, t.dst(buf))
				}
				return buf, nil
			}, func(link any) (*Routine, error) {
				buf := link.(*routinePreloadBuf)

				// Same rule as the reflection mapper's row validator: the child
				// exists only if at least one of its columns is not NULL. Columns
				// not selected by the query are never scanned and stay invalid,
				// so this check also matches when only a subset is selected.
				if !(buf.ID.IsValue()) &&
					!(buf.UserID.IsValue()) &&
					!(buf.Title.IsValue()) &&
					!(buf.CreatedAt.IsValue()) &&
					!(buf.DeletedAt.IsValue()) &&
					!(buf.ExerciseOrder.IsValue()) {
					return nil, nil
				}

				o := new(Routine)
				if buf.ID.IsValue() {
					o.ID = buf.ID.MustGet()
				}
				if buf.UserID.IsValue() {
					o.UserID = buf.UserID.MustGet()
				}
				if buf.Title.IsValue() {
					o.Title = buf.Title.MustGet()
				}
				if buf.CreatedAt.IsValue() {
					o.CreatedAt = buf.CreatedAt.MustGet()
				}
				o.DeletedAt = buf.DeletedAt
				if buf.ExerciseOrder.IsValue() {
					o.ExerciseOrder = buf.ExerciseOrder.MustGet()
				}
				return o, nil
			}
	}
}

func (o *Routine) Preload(name string, retrieved any) error {
	if o == nil {
		return nil
	}

	switch name {
	case "Exercises":
		rels, ok := retrieved.(ExerciseSlice)
		if !ok {
			return fmt.Errorf("routine cannot load %T as %q", retrieved, name)
		}

		o.R.Exercises = rels
		o.R.Loaded.Exercises = true

		for _, rel := range rels {
			if rel != nil {
				rel.R.Routines = RoutineSlice{o}
			}
		}
		return nil
	case "PlanRoutines":
		rels, ok := retrieved.(PlanRoutineSlice)
		if !ok {
			return fmt.Errorf("routine cannot load %T as %q", retrieved, name)
		}

		o.R.PlanRoutines = rels
		o.R.Loaded.PlanRoutines = true

		for _, rel := range rels {
			if rel != nil {
				rel.R.Routine = o
				rel.R.Loaded.Routine = true
			}
		}
		return nil
	case "User":
		rel, ok := retrieved.(*User)
		if !ok {
			return fmt.Errorf("routine cannot load %T as %q", retrieved, name)
		}

		o.R.User = rel
		o.R.Loaded.User = true

		if rel != nil {
			rel.R.Routines = RoutineSlice{o}
		}
		return nil
	case "Workouts":
		rels, ok := retrieved.(WorkoutSlice)
		if !ok {
			return fmt.Errorf("routine cannot load %T as %q", retrieved, name)
		}

		o.R.Workouts = rels
		o.R.Loaded.Workouts = true

		for _, rel := range rels {
			if rel != nil {
				rel.R.Routine = o
				rel.R.Loaded.Routine = true
			}
		}
		return nil
	default:
		return fmt.Errorf("routine has no relationship %q", name)
	}
}

type routinePreloader struct {
	User func(...psql.PreloadOption) psql.Preloader
}

func buildRoutinePreloader() routinePreloader {
	return routinePreloader{
		User: func(opts ...psql.PreloadOption) psql.Preloader {
			return psql.Preload[*User, UserSlice](psql.PreloadRel{
				Name: "User",
				Sides: []psql.PreloadSide{
					{
						From:        Routines,
						To:          Users,
						FromColumns: []string{"user_id"},
						ToColumns:   []string{"id"},
					},
				},
			}, Users.Columns.Names(), userScanMapperNullable, opts...)
		},
	}
}

type routineThenLoader[Q orm.Loadable] struct {
	Exercises    func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
	PlanRoutines func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
	User         func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
	Workouts     func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
}

func buildRoutineThenLoader[Q orm.Loadable]() routineThenLoader[Q] {
	type ExercisesLoadInterface interface {
		LoadExercises(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}
	type PlanRoutinesLoadInterface interface {
		LoadPlanRoutines(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}
	type UserLoadInterface interface {
		LoadUser(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}
	type WorkoutsLoadInterface interface {
		LoadWorkouts(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}

	return routineThenLoader[Q]{
		Exercises: thenLoadBuilder[Q](
			"Exercises",
			func(ctx context.Context, exec bob.Executor, retrieved ExercisesLoadInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadExercises(ctx, exec, mods...)
			},
		),
		PlanRoutines: thenLoadBuilder[Q](
			"PlanRoutines",
			func(ctx context.Context, exec bob.Executor, retrieved PlanRoutinesLoadInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadPlanRoutines(ctx, exec, mods...)
			},
		),
		User: thenLoadBuilder[Q](
			"User",
			func(ctx context.Context, exec bob.Executor, retrieved UserLoadInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadUser(ctx, exec, mods...)
			},
		),
		Workouts: thenLoadBuilder[Q](
			"Workouts",
			func(ctx context.Context, exec bob.Executor, retrieved WorkoutsLoadInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadWorkouts(ctx, exec, mods...)
			},
		),
	}
}

// LoadExercises loads the routine's Exercises into the .R struct
func (o *Routine) LoadExercises(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.Exercises = nil
	o.R.Loaded.Exercises = false

	related, err := o.Exercises(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, rel := range related {
		rel.R.Routines = RoutineSlice{o}
	}

	o.R.Exercises = related
	o.R.Loaded.Exercises = true
	return nil
}

// LoadExercises loads the routine's Exercises into the .R struct
func (os RoutineSlice) LoadExercises(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	q := os.Exercises(append(
		mods,
		// since we are changing the columns, the defaults must be added if the
		// original mods did not set any; this runs after the user mods and
		// before the related_* columns below, so it sees exactly what they set
		bob.ModFunc[*dialect.SelectQuery](func(q *dialect.SelectQuery) {
			if len(q.SelectList.Columns) == 0 {
				sm.Columns(Exercises.Columns).Apply(q)
			}
		}),
		sm.Columns(ExercisesRoutines.Columns.RoutineID.As("related_routines.ID")),
	)...)

	IDSlice := make([]string, 0, len(os))

	mapper := scan.Mod(q.Scanner, func(ctx context.Context, cols []string) (scan.BeforeFunc, func(any, any) error) {
		// Resolve each joined key column name to its index once per query. The
		// previous code scanned by name on every row, which meant a linear column
		// search per row; the columns are added by this loader so they are always
		// present, but an unselected column simply stays unresolved and is skipped.
		IDIdx := -1
		for i, col := range cols {
			switch col {
			case "related_routines.ID":
				IDIdx = i
			}
		}

		return func(row *scan.Row) (any, error) {
				IDSlice = append(IDSlice, *new(string))
				if IDIdx >= 0 {
					row.ScheduleScanByIndex(IDIdx, &IDSlice[len(IDSlice)-1])
				}

				return nil, nil
			},
			func(any, any) error {
				return nil
			}
	})

	exercises, err := bob.Allx[bob.SliceTransformer[*Exercise, ExerciseSlice]](ctx, exec, q, mapper)
	if err != nil {
		return err
	}

	for _, o := range os {
		if o == nil {
			continue
		}
		o.R.Exercises = nil
		o.R.Loaded.Exercises = true
	}
	// O(N+M) stitch via a map; child key is the carried slice IDSlice[i] (was O(N*M)).
	routineByKey := make(map[string][]*Routine, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}

		routineByKey[o.ID] = append(routineByKey[o.ID], o)
	}

	for i, rel := range exercises {
		owners, ok := routineByKey[IDSlice[i]]
		if !ok {
			continue
		}

		for _, o := range owners {

			rel.R.Routines = append(rel.R.Routines, o)

			o.R.Exercises = append(o.R.Exercises, rel)

		}
	}

	return nil
}

// LoadPlanRoutines loads the routine's PlanRoutines into the .R struct
func (o *Routine) LoadPlanRoutines(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.PlanRoutines = nil
	o.R.Loaded.PlanRoutines = false

	related, err := o.PlanRoutines(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, rel := range related {
		rel.R.Routine = o
		rel.R.Loaded.Routine = true
	}

	o.R.PlanRoutines = related
	o.R.Loaded.PlanRoutines = true
	return nil
}

// LoadPlanRoutines loads the routine's PlanRoutines into the .R struct
func (os RoutineSlice) LoadPlanRoutines(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	planRoutines, err := os.PlanRoutines(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, o := range os {
		if o == nil {
			continue
		}

		o.R.PlanRoutines = nil
		o.R.Loaded.PlanRoutines = true
	}
	// O(N+M) stitch via a map keyed by the join column (key -> []parent; was O(N*M)).
	routineByKey := make(map[string][]*Routine, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}

		routineByKey[o.ID] = append(routineByKey[o.ID], o)
	}

	for _, rel := range planRoutines {

		owners, ok := routineByKey[rel.RoutineID]
		if !ok {
			continue
		}

		for _, o := range owners {

			rel.R.Routine = o
			rel.R.Loaded.Routine = true

			o.R.PlanRoutines = append(o.R.PlanRoutines, rel)

		}
	}

	return nil
}

// LoadUser loads the routine's User into the .R struct
func (o *Routine) LoadUser(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.User = nil
	o.R.Loaded.User = false

	related, err := o.User(mods...).One(ctx, exec)
	if err != nil {
		return err
	}

	related.R.Routines = RoutineSlice{o}

	o.R.User = related
	o.R.Loaded.User = true
	return nil
}

// LoadUser loads the routine's User into the .R struct
func (os RoutineSlice) LoadUser(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	users, err := os.User(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, o := range os {
		if o == nil {
			continue
		}

		o.R.User = nil
		o.R.Loaded.User = true
	}
	// O(N+M) stitch via a map keyed by the join column (key -> []parent; was O(N*M)).
	routineByKey := make(map[string][]*Routine, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}

		routineByKey[o.UserID] = append(routineByKey[o.UserID], o)
	}

	for _, rel := range users {

		owners, ok := routineByKey[rel.ID]
		if !ok {
			continue
		}

		for _, o := range owners {

			// to-one: keep only the first matching child (matches the previous break)
			if o.R.User != nil {
				continue
			}

			rel.R.Routines = append(rel.R.Routines, o)

			o.R.User = rel

		}
	}

	return nil
}

// LoadWorkouts loads the routine's Workouts into the .R struct
func (o *Routine) LoadWorkouts(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.Workouts = nil
	o.R.Loaded.Workouts = false

	related, err := o.Workouts(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, rel := range related {
		rel.R.Routine = o
		rel.R.Loaded.Routine = true
	}

	o.R.Workouts = related
	o.R.Loaded.Workouts = true
	return nil
}

// LoadWorkouts loads the routine's Workouts into the .R struct
func (os RoutineSlice) LoadWorkouts(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	workouts, err := os.Workouts(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, o := range os {
		if o == nil {
			continue
		}

		o.R.Workouts = nil
		o.R.Loaded.Workouts = true
	}
	// O(N+M) stitch via a map keyed by the join column (key -> []parent; was O(N*M)).
	routineByKey := make(map[string][]*Routine, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}

		routineByKey[o.ID] = append(routineByKey[o.ID], o)
	}

	for _, rel := range workouts {

		if !rel.RoutineID.IsValue() {
			continue
		}

		owners, ok := routineByKey[rel.RoutineID.MustGet()]
		if !ok {
			continue
		}

		for _, o := range owners {

			rel.R.Routine = o
			rel.R.Loaded.Routine = true

			o.R.Workouts = append(o.R.Workouts, rel)

		}
	}

	return nil
}

// routineC is where relationship counts are stored.
type routineC struct {
	Exercises    *int64
	PlanRoutines *int64
	Workouts     *int64
}

// PreloadCount sets a count in the C struct by name
func (o *Routine) PreloadCount(name string, count int64) error {
	if o == nil {
		return nil
	}

	switch name {
	case "Exercises":
		o.C.Exercises = &count
	case "PlanRoutines":
		o.C.PlanRoutines = &count
	case "Workouts":
		o.C.Workouts = &count
	}
	return nil
}

type routineCountPreloader struct {
	Exercises    func(...bob.Mod[*dialect.SelectQuery]) psql.Preloader
	PlanRoutines func(...bob.Mod[*dialect.SelectQuery]) psql.Preloader
	Workouts     func(...bob.Mod[*dialect.SelectQuery]) psql.Preloader
}

func buildRoutineCountPreloader() routineCountPreloader {
	return routineCountPreloader{
		Exercises: func(mods ...bob.Mod[*dialect.SelectQuery]) psql.Preloader {
			return countPreloader[*Routine]("Exercises", func(parent string) bob.Expression {
				// Build a correlated subquery: (SELECT COUNT(*) FROM related WHERE fk = parent.pk)
				if parent == "" {
					parent = Routines.Alias()
				}

				subqueryMods := []bob.Mod[*dialect.SelectQuery]{
					sm.Columns(psql.Raw("count(*)")),

					sm.From(ExercisesRoutines.NameAsExpr()),
					sm.Where(psql.Quote(ExercisesRoutines.Alias(), "routine_id").EQ(psql.Quote(parent, "id"))),
					sm.InnerJoin(Exercises.NameAsExpr()).On(
						psql.Quote(Exercises.Alias(), "id").EQ(psql.Quote(ExercisesRoutines.Alias(), "exercise_id")),
					),
				}
				subqueryMods = append(subqueryMods, mods...)
				return psql.Group(psql.Select(subqueryMods...).Expression)
			})
		},
		PlanRoutines: func(mods ...bob.Mod[*dialect.SelectQuery]) psql.Preloader {
			return countPreloader[*Routine]("PlanRoutines", func(parent string) bob.Expression {
				// Build a correlated subquery: (SELECT COUNT(*) FROM related WHERE fk = parent.pk)
				if parent == "" {
					parent = Routines.Alias()
				}

				subqueryMods := []bob.Mod[*dialect.SelectQuery]{
					sm.Columns(psql.Raw("count(*)")),

					sm.From(PlanRoutines.NameAsExpr()),
					sm.Where(psql.Quote(PlanRoutines.Alias(), "routine_id").EQ(psql.Quote(parent, "id"))),
				}
				subqueryMods = append(subqueryMods, mods...)
				return psql.Group(psql.Select(subqueryMods...).Expression)
			})
		},
		Workouts: func(mods ...bob.Mod[*dialect.SelectQuery]) psql.Preloader {
			return countPreloader[*Routine]("Workouts", func(parent string) bob.Expression {
				// Build a correlated subquery: (SELECT COUNT(*) FROM related WHERE fk = parent.pk)
				if parent == "" {
					parent = Routines.Alias()
				}

				subqueryMods := []bob.Mod[*dialect.SelectQuery]{
					sm.Columns(psql.Raw("count(*)")),

					sm.From(Workouts.NameAsExpr()),
					sm.Where(psql.Quote(Workouts.Alias(), "routine_id").EQ(psql.Quote(parent, "id"))),
				}
				subqueryMods = append(subqueryMods, mods...)
				return psql.Group(psql.Select(subqueryMods...).Expression)
			})
		},
	}
}

type routineCountThenLoader[Q orm.Loadable] struct {
	Exercises    func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
	PlanRoutines func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
	Workouts     func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
}

func buildRoutineCountThenLoader[Q orm.Loadable]() routineCountThenLoader[Q] {
	type ExercisesCountInterface interface {
		LoadCountExercises(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}
	type PlanRoutinesCountInterface interface {
		LoadCountPlanRoutines(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}
	type WorkoutsCountInterface interface {
		LoadCountWorkouts(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}

	return routineCountThenLoader[Q]{
		Exercises: countThenLoadBuilder[Q](
			"Exercises",
			func(ctx context.Context, exec bob.Executor, retrieved ExercisesCountInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadCountExercises(ctx, exec, mods...)
			},
		),
		PlanRoutines: countThenLoadBuilder[Q](
			"PlanRoutines",
			func(ctx context.Context, exec bob.Executor, retrieved PlanRoutinesCountInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadCountPlanRoutines(ctx, exec, mods...)
			},
		),
		Workouts: countThenLoadBuilder[Q](
			"Workouts",
			func(ctx context.Context, exec bob.Executor, retrieved WorkoutsCountInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadCountWorkouts(ctx, exec, mods...)
			},
		),
	}
}

// LoadCountExercises loads the count of Exercises into the C struct
func (o *Routine) LoadCountExercises(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	count, err := o.Exercises(mods...).Count(ctx, exec)
	if err != nil {
		return err
	}

	o.C.Exercises = &count
	return nil
}

// LoadCountExercises loads the count of Exercises for a slice in a single batch query
func (os RoutineSlice) LoadCountExercises(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	// Build the IN arg expression from parent PKs

	pkID := make(pgtypes.Array[string], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}
	PKArgExpr := psql.Any(psql.Cast(psql.Arg(pkID), "uuid[]"))

	// countResult holds one scanned row from the batch count query.
	// FK columns are aliased to the parent PK column names for direct map lookup.
	type countResult struct {
		ID    string
		Count int64
	}

	batchMods := []bob.Mod[*dialect.SelectQuery]{
		// SELECT fk AS parent_pk, count(*)
		sm.Columns(
			ExercisesRoutines.Columns.RoutineID.As("id"),
			psql.Raw("count(*) as count"),
		),
		// Multi-hop: FROM first join table, JOIN through to final related table
		sm.From(ExercisesRoutines.NameAsExpr()),
		sm.InnerJoin(Exercises.NameAsExpr()).On(
			Exercises.Columns.ID.EQ(ExercisesRoutines.Columns.ExerciseID),
		),

		// WHERE fk IN (parent PKs) — psql single-column FK uses `= ANY(array)` (see PKArgExpr above)
		sm.Where(ExercisesRoutines.Columns.RoutineID.EQ(PKArgExpr)),
		// GROUP BY fk columns
		sm.GroupBy(ExercisesRoutines.Columns.RoutineID),
	}
	batchMods = append(batchMods, mods...)

	results, err := bob.All(ctx, exec,
		psql.Select(batchMods...),
		scan.StructMapper[countResult](),
	)
	if err != nil {
		return err
	}

	// Single-column FK: direct map lookup
	countMap := make(map[string]int64, len(results))
	for _, r := range results {
		countMap[r.ID] = r.Count
	}
	for _, o := range os {
		if o == nil {
			continue
		}
		count := countMap[o.ID]
		o.C.Exercises = &count
	}

	return nil
}

// LoadCountPlanRoutines loads the count of PlanRoutines into the C struct
func (o *Routine) LoadCountPlanRoutines(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	count, err := o.PlanRoutines(mods...).Count(ctx, exec)
	if err != nil {
		return err
	}

	o.C.PlanRoutines = &count
	return nil
}

// LoadCountPlanRoutines loads the count of PlanRoutines for a slice in a single batch query
func (os RoutineSlice) LoadCountPlanRoutines(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	// Build the IN arg expression from parent PKs

	pkID := make(pgtypes.Array[string], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}
	PKArgExpr := psql.Any(psql.Cast(psql.Arg(pkID), "uuid[]"))

	// countResult holds one scanned row from the batch count query.
	// FK columns are aliased to the parent PK column names for direct map lookup.
	type countResult struct {
		ID    string
		Count int64
	}

	batchMods := []bob.Mod[*dialect.SelectQuery]{
		// SELECT fk AS parent_pk, count(*)
		sm.Columns(
			PlanRoutines.Columns.RoutineID.As("id"),
			psql.Raw("count(*) as count"),
		),
		// Single-hop: FROM related table directly
		sm.From(PlanRoutines.NameAsExpr()),

		// WHERE fk IN (parent PKs) — psql single-column FK uses `= ANY(array)` (see PKArgExpr above)
		sm.Where(PlanRoutines.Columns.RoutineID.EQ(PKArgExpr)),
		// GROUP BY fk columns
		sm.GroupBy(PlanRoutines.Columns.RoutineID),
	}
	batchMods = append(batchMods, mods...)

	results, err := bob.All(ctx, exec,
		psql.Select(batchMods...),
		scan.StructMapper[countResult](),
	)
	if err != nil {
		return err
	}

	// Single-column FK: direct map lookup
	countMap := make(map[string]int64, len(results))
	for _, r := range results {
		countMap[r.ID] = r.Count
	}
	for _, o := range os {
		if o == nil {
			continue
		}
		count := countMap[o.ID]
		o.C.PlanRoutines = &count
	}

	return nil
}

// LoadCountWorkouts loads the count of Workouts into the C struct
func (o *Routine) LoadCountWorkouts(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	count, err := o.Workouts(mods...).Count(ctx, exec)
	if err != nil {
		return err
	}

	o.C.Workouts = &count
	return nil
}

// LoadCountWorkouts loads the count of Workouts for a slice in a single batch query
func (os RoutineSlice) LoadCountWorkouts(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	// Build the IN arg expression from parent PKs

	pkID := make(pgtypes.Array[string], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}
	PKArgExpr := psql.Any(psql.Cast(psql.Arg(pkID), "uuid[]"))

	// countResult holds one scanned row from the batch count query.
	// FK columns are aliased to the parent PK column names for direct map lookup.
	type countResult struct {
		ID    string
		Count int64
	}

	batchMods := []bob.Mod[*dialect.SelectQuery]{
		// SELECT fk AS parent_pk, count(*)
		sm.Columns(
			Workouts.Columns.RoutineID.As("id"),
			psql.Raw("count(*) as count"),
		),
		// Single-hop: FROM related table directly
		sm.From(Workouts.NameAsExpr()),

		// WHERE fk IN (parent PKs) — psql single-column FK uses `= ANY(array)` (see PKArgExpr above)
		sm.Where(Workouts.Columns.RoutineID.EQ(PKArgExpr)),
		// GROUP BY fk columns
		sm.GroupBy(Workouts.Columns.RoutineID),
	}
	batchMods = append(batchMods, mods...)

	results, err := bob.All(ctx, exec,
		psql.Select(batchMods...),
		scan.StructMapper[countResult](),
	)
	if err != nil {
		return err
	}

	// Single-column FK: direct map lookup
	countMap := make(map[string]int64, len(results))
	for _, r := range results {
		countMap[r.ID] = r.Count
	}
	for _, o := range os {
		if o == nil {
			continue
		}
		count := countMap[o.ID]
		o.C.Workouts = &count
	}

	return nil
}

type routineJoins[Q dialect.Joinable] struct {
	typ          string
	Exercises    modAs[Q, exerciseColumns]
	PlanRoutines modAs[Q, planRoutineColumns]
	User         modAs[Q, userColumns]
	Workouts     modAs[Q, workoutColumns]
}

func (j routineJoins[Q]) aliasedAs(alias string) routineJoins[Q] {
	return buildRoutineJoins[Q](buildRoutineColumns(alias), j.typ)
}

func buildRoutineJoins[Q dialect.Joinable](cols routineColumns, typ string) routineJoins[Q] {
	return routineJoins[Q]{
		typ: typ,
		Exercises: modAs[Q, exerciseColumns]{
			c: Exercises.Columns,
			f: func(to exerciseColumns) bob.Mod[Q] {
				uniqueSuffix := strconv.FormatUint(bob.NextUniqueInt(), 10)
				mods := make(mods.QueryMods[Q], 0, 2)

				{
					to := ExercisesRoutines.Columns.AliasedAs(ExercisesRoutines.Columns.Alias() + uniqueSuffix)
					mods = append(mods, dialect.Join[Q](typ, ExercisesRoutines.NameExpr().As(to.Alias())).On(
						to.RoutineID.EQ(cols.ID),
					))
				}
				{
					cols := ExercisesRoutines.Columns.AliasedAs(ExercisesRoutines.Columns.Alias() + uniqueSuffix)
					mods = append(mods, dialect.Join[Q](typ, Exercises.NameExpr().As(to.Alias())).On(
						to.ID.EQ(cols.ExerciseID),
					))
				}

				return mods
			},
		},
		PlanRoutines: modAs[Q, planRoutineColumns]{
			c: PlanRoutines.Columns,
			f: func(to planRoutineColumns) bob.Mod[Q] {
				mods := make(mods.QueryMods[Q], 0, 1)

				{
					mods = append(mods, dialect.Join[Q](typ, PlanRoutines.NameExpr().As(to.Alias())).On(
						to.RoutineID.EQ(cols.ID),
					))
				}

				return mods
			},
		},
		User: modAs[Q, userColumns]{
			c: Users.Columns,
			f: func(to userColumns) bob.Mod[Q] {
				mods := make(mods.QueryMods[Q], 0, 1)

				{
					mods = append(mods, dialect.Join[Q](typ, Users.NameExpr().As(to.Alias())).On(
						to.ID.EQ(cols.UserID),
					))
				}

				return mods
			},
		},
		Workouts: modAs[Q, workoutColumns]{
			c: Workouts.Columns,
			f: func(to workoutColumns) bob.Mod[Q] {
				mods := make(mods.QueryMods[Q], 0, 1)

				{
					mods = append(mods, dialect.Join[Q](typ, Workouts.NameExpr().As(to.Alias())).On(
						to.RoutineID.EQ(cols.ID),
					))
				}

				return mods
			},
		},
	}
}
