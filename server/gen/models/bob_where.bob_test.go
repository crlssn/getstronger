// Code generated by BobGen psql v0.50.0. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"strings"
	"testing"

	"github.com/stephenafamo/bob"
	"github.com/stephenafamo/bob/dialect/psql"
	"github.com/stephenafamo/bob/dialect/psql/sm"
)

// TestAuthHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestAuthHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("User", func(t *testing.T) {
		q := psql.Select(
			sm.From(Auths.NameExpr()),
			SelectWhere.Auths.R.HasUser(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUser: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUser: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestExerciseHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestExerciseHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("User", func(t *testing.T) {
		q := psql.Select(
			sm.From(Exercises.NameExpr()),
			SelectWhere.Exercises.R.HasUser(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUser: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUser: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("ExercisesRoutines", func(t *testing.T) {
		q := psql.Select(
			sm.From(Exercises.NameExpr()),
			SelectWhere.Exercises.R.HasExercisesRoutines(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasExercisesRoutines: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasExercisesRoutines: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Routines", func(t *testing.T) {
		q := psql.Select(
			sm.From(Exercises.NameExpr()),
			SelectWhere.Exercises.R.HasRoutines(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasRoutines: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasRoutines: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Sets", func(t *testing.T) {
		q := psql.Select(
			sm.From(Exercises.NameExpr()),
			SelectWhere.Exercises.R.HasSets(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSets: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSets: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("WorkoutGroupExercises", func(t *testing.T) {
		q := psql.Select(
			sm.From(Exercises.NameExpr()),
			SelectWhere.Exercises.R.HasWorkoutGroupExercises(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasWorkoutGroupExercises: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasWorkoutGroupExercises: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestExercisesRoutineHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestExercisesRoutineHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("GroupRoutineGroup", func(t *testing.T) {
		q := psql.Select(
			sm.From(ExercisesRoutines.NameExpr()),
			SelectWhere.ExercisesRoutines.R.HasGroupRoutineGroup(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGroupRoutineGroup: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGroupRoutineGroup: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Exercise", func(t *testing.T) {
		q := psql.Select(
			sm.From(ExercisesRoutines.NameExpr()),
			SelectWhere.ExercisesRoutines.R.HasExercise(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasExercise: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasExercise: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Routine", func(t *testing.T) {
		q := psql.Select(
			sm.From(ExercisesRoutines.NameExpr()),
			SelectWhere.ExercisesRoutines.R.HasRoutine(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasRoutine: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasRoutine: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestFollowerHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestFollowerHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("FolloweeUser", func(t *testing.T) {
		q := psql.Select(
			sm.From(Followers.NameExpr()),
			SelectWhere.Followers.R.HasFolloweeUser(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasFolloweeUser: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasFolloweeUser: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("FollowerUser", func(t *testing.T) {
		q := psql.Select(
			sm.From(Followers.NameExpr()),
			SelectWhere.Followers.R.HasFollowerUser(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasFollowerUser: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasFollowerUser: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestNotificationHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestNotificationHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("User", func(t *testing.T) {
		q := psql.Select(
			sm.From(Notifications.NameExpr()),
			SelectWhere.Notifications.R.HasUser(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUser: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUser: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestPlanRoutineHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestPlanRoutineHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("Plan", func(t *testing.T) {
		q := psql.Select(
			sm.From(PlanRoutines.NameExpr()),
			SelectWhere.PlanRoutines.R.HasPlan(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasPlan: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasPlan: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Routine", func(t *testing.T) {
		q := psql.Select(
			sm.From(PlanRoutines.NameExpr()),
			SelectWhere.PlanRoutines.R.HasRoutine(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasRoutine: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasRoutine: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestPlanHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestPlanHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("PlanRoutines", func(t *testing.T) {
		q := psql.Select(
			sm.From(Plans.NameExpr()),
			SelectWhere.Plans.R.HasPlanRoutines(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasPlanRoutines: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasPlanRoutines: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("User", func(t *testing.T) {
		q := psql.Select(
			sm.From(Plans.NameExpr()),
			SelectWhere.Plans.R.HasUser(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUser: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUser: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestRoutineGroupHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestRoutineGroupHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("GroupExercisesRoutines", func(t *testing.T) {
		q := psql.Select(
			sm.From(RoutineGroups.NameExpr()),
			SelectWhere.RoutineGroups.R.HasGroupExercisesRoutines(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGroupExercisesRoutines: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGroupExercisesRoutines: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Routine", func(t *testing.T) {
		q := psql.Select(
			sm.From(RoutineGroups.NameExpr()),
			SelectWhere.RoutineGroups.R.HasRoutine(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasRoutine: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasRoutine: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestRoutineHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestRoutineHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("ExercisesRoutines", func(t *testing.T) {
		q := psql.Select(
			sm.From(Routines.NameExpr()),
			SelectWhere.Routines.R.HasExercisesRoutines(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasExercisesRoutines: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasExercisesRoutines: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("PlanRoutines", func(t *testing.T) {
		q := psql.Select(
			sm.From(Routines.NameExpr()),
			SelectWhere.Routines.R.HasPlanRoutines(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasPlanRoutines: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasPlanRoutines: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("RoutineGroups", func(t *testing.T) {
		q := psql.Select(
			sm.From(Routines.NameExpr()),
			SelectWhere.Routines.R.HasRoutineGroups(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasRoutineGroups: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasRoutineGroups: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("User", func(t *testing.T) {
		q := psql.Select(
			sm.From(Routines.NameExpr()),
			SelectWhere.Routines.R.HasUser(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUser: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUser: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Exercises", func(t *testing.T) {
		q := psql.Select(
			sm.From(Routines.NameExpr()),
			SelectWhere.Routines.R.HasExercises(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasExercises: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasExercises: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Workouts", func(t *testing.T) {
		q := psql.Select(
			sm.From(Routines.NameExpr()),
			SelectWhere.Routines.R.HasWorkouts(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasWorkouts: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasWorkouts: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestSetHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestSetHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("Exercise", func(t *testing.T) {
		q := psql.Select(
			sm.From(Sets.NameExpr()),
			SelectWhere.Sets.R.HasExercise(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasExercise: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasExercise: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("WorkoutGroupExercise", func(t *testing.T) {
		q := psql.Select(
			sm.From(Sets.NameExpr()),
			SelectWhere.Sets.R.HasWorkoutGroupExercise(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasWorkoutGroupExercise: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasWorkoutGroupExercise: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Workout", func(t *testing.T) {
		q := psql.Select(
			sm.From(Sets.NameExpr()),
			SelectWhere.Sets.R.HasWorkout(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasWorkout: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasWorkout: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestUserHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestUserHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("Exercises", func(t *testing.T) {
		q := psql.Select(
			sm.From(Users.NameExpr()),
			SelectWhere.Users.R.HasExercises(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasExercises: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasExercises: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Users", func(t *testing.T) {
		q := psql.Select(
			sm.From(Users.NameExpr()),
			SelectWhere.Users.R.HasUsers(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUsers: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUsers: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Notifications", func(t *testing.T) {
		q := psql.Select(
			sm.From(Users.NameExpr()),
			SelectWhere.Users.R.HasNotifications(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasNotifications: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasNotifications: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Plans", func(t *testing.T) {
		q := psql.Select(
			sm.From(Users.NameExpr()),
			SelectWhere.Users.R.HasPlans(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasPlans: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasPlans: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Routines", func(t *testing.T) {
		q := psql.Select(
			sm.From(Users.NameExpr()),
			SelectWhere.Users.R.HasRoutines(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasRoutines: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasRoutines: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Auth", func(t *testing.T) {
		q := psql.Select(
			sm.From(Users.NameExpr()),
			SelectWhere.Users.R.HasAuth(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasAuth: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasAuth: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("WorkoutComments", func(t *testing.T) {
		q := psql.Select(
			sm.From(Users.NameExpr()),
			SelectWhere.Users.R.HasWorkoutComments(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasWorkoutComments: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasWorkoutComments: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Workouts", func(t *testing.T) {
		q := psql.Select(
			sm.From(Users.NameExpr()),
			SelectWhere.Users.R.HasWorkouts(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasWorkouts: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasWorkouts: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestWorkoutCommentHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestWorkoutCommentHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("User", func(t *testing.T) {
		q := psql.Select(
			sm.From(WorkoutComments.NameExpr()),
			SelectWhere.WorkoutComments.R.HasUser(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUser: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUser: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Workout", func(t *testing.T) {
		q := psql.Select(
			sm.From(WorkoutComments.NameExpr()),
			SelectWhere.WorkoutComments.R.HasWorkout(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasWorkout: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasWorkout: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestWorkoutGroupExerciseHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestWorkoutGroupExerciseHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("Sets", func(t *testing.T) {
		q := psql.Select(
			sm.From(WorkoutGroupExercises.NameExpr()),
			SelectWhere.WorkoutGroupExercises.R.HasSets(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSets: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSets: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Exercise", func(t *testing.T) {
		q := psql.Select(
			sm.From(WorkoutGroupExercises.NameExpr()),
			SelectWhere.WorkoutGroupExercises.R.HasExercise(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasExercise: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasExercise: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("WorkoutGroup", func(t *testing.T) {
		q := psql.Select(
			sm.From(WorkoutGroupExercises.NameExpr()),
			SelectWhere.WorkoutGroupExercises.R.HasWorkoutGroup(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasWorkoutGroup: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasWorkoutGroup: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestWorkoutGroupHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestWorkoutGroupHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("WorkoutGroupExercises", func(t *testing.T) {
		q := psql.Select(
			sm.From(WorkoutGroups.NameExpr()),
			SelectWhere.WorkoutGroups.R.HasWorkoutGroupExercises(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasWorkoutGroupExercises: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasWorkoutGroupExercises: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Workout", func(t *testing.T) {
		q := psql.Select(
			sm.From(WorkoutGroups.NameExpr()),
			SelectWhere.WorkoutGroups.R.HasWorkout(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasWorkout: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasWorkout: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestWorkoutHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestWorkoutHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("Sets", func(t *testing.T) {
		q := psql.Select(
			sm.From(Workouts.NameExpr()),
			SelectWhere.Workouts.R.HasSets(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSets: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSets: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("WorkoutComments", func(t *testing.T) {
		q := psql.Select(
			sm.From(Workouts.NameExpr()),
			SelectWhere.Workouts.R.HasWorkoutComments(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasWorkoutComments: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasWorkoutComments: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("WorkoutGroups", func(t *testing.T) {
		q := psql.Select(
			sm.From(Workouts.NameExpr()),
			SelectWhere.Workouts.R.HasWorkoutGroups(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasWorkoutGroups: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasWorkoutGroups: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Routine", func(t *testing.T) {
		q := psql.Select(
			sm.From(Workouts.NameExpr()),
			SelectWhere.Workouts.R.HasRoutine(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasRoutine: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasRoutine: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("User", func(t *testing.T) {
		q := psql.Select(
			sm.From(Workouts.NameExpr()),
			SelectWhere.Workouts.R.HasUser(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUser: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUser: expected EXISTS in query, got: %s", sql)
		}
	})
}
